# Copy to .env for local dev, or set as an Environment Variable in the
# Vercel dashboard for the client project (Production/Preview/Development).
# Leave unset locally to use the vite dev proxy to http://localhost:5000.
VITE_API_URL=https://your-backend-project.vercel.app/api
